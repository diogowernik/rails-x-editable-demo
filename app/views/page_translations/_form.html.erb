<%= form_with(model: page_translation, local: true) do |form| %>
  <% if page_translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_translation.errors.count, "error") %> prohibited this page_translation from being saved:</h2>

      <ul>
      <% page_translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
